import { createStyles, Theme } from "@material-ui/core";

const style = (theme: Theme) =>
  createStyles({
    root: {
      display: "block",
      width: "100vw",
    },
  });

export default style;
